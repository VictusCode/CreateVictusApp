type RepositoryConfigType = {
  tarURL: string;
};

const repositoryConfig: RepositoryConfigType = Object.freeze({
  tarURL: 'https://codeload.github.com/VictusCode/CreateVictusApp/tar.gz/main',
});

export { repositoryConfig };
