import { mkdirSync } from 'fs';

const copyFromURLtoDir = async (url: string, dir: string) => {
  // url = https://github.com/VictusCode/CreateVictusApp/tree/main/template/frontend/base
  // dir = TEST_FOLDER
};

export { copyFromURLtoDir };
